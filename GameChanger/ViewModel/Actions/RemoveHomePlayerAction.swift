//
//  WalkAction.swift
//  GameChanger
//
//  Created by Pavan Kumar Reddy on 23/02/26.
//

import Foundation

/*
struct RemoveHomePlayerAction: GameCommand {
    func execute(on state: GameState) -> GameState{
        var state = state
        if let index = state.basePlayers.firstIndex(where: { $0.base == .home }) {
            print("Removed home player \(state.basePlayers[index].name)")
            state.basePlayers.remove(at: index)
        }
        return state
    }
}
*/
